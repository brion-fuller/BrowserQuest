{
  "name": "@browserquest/browserquest",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "start": "yarn workspaces foreach -pti run start"
  }
}
