{
  "name": "BrowserQuest",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build:client": "npm run build -w @browserquest/client",
    "start:server": "npm run start -w @browserquest/server"
  }
}
